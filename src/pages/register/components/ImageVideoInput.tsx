import VImageVideoInput from './VImageVideoInput';
import { ImageVideoInputProps } from '../registerType';

const ImageVideoInput = (props: ImageVideoInputProps) => {
  return <VImageVideoInput {...props} />;
};

export default ImageVideoInput;
